export enum DocStatus {
  submitted = 'submitted',
  selected = 'selected', //selected_for_review = 'selected_for_review',
  //under_review = 'under_review',
  reviewed = 'reviewed',
  accepted = 'accepted',
  rejected = 'rejected'
}