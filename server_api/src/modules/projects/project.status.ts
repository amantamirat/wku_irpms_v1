export enum ProjectStatus {
    pending = 'pending',
    submitted = "submitted",
    rejected = "rejected",
    accepted = "accepted",
    negotiation = "negotiation",
    approved = 'approved',
    granted = 'granted',
    completed = 'completed'
}