export enum ProjectStatus {
    pending = 'pending',//draft
    submitted = "submitted",
    rejected = "rejected",
    accepted = "accepted",
    negotiation = "negotiation",
    approved = 'approved',
    granted = 'granted',
    completed = 'completed'
}