export enum ThemeType {
    catalog = 'Catalog',
    theme = 'Theme',
    componenet = 'Componenet',
    focusArea = 'Focus-area'
}

export enum ThemeLevel {
    broad = 'Broad',
    componenet = 'Componenet',
    narrow = 'Narrow',
}