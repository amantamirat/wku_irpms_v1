'use client';

import ProjectManager from './components/ProjectManager';

const ProjectPage = () => {
    return (
        <div className="card">
            <ProjectManager />
        </div>
    );
};

export default ProjectPage;