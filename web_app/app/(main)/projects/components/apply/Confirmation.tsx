import { Project } from "../../models/project.model";

interface ConfirmationProps {
    project: Project;
}
const Confirmation = ({ project }: ConfirmationProps) => {
    return (<></>);
}
export default Confirmation;